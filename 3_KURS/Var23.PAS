{определенный интеграл x*e^(-x)
методом левых и правых прямоугольников
в интервале 0 - 2 при n=10 (10 разбиений)

Суть метода:
Опред. шаг s=(a-b)/n,a и b -пределы интегрирования,
n- кол-во разбиений;
проводим линии в точках разбиения;
строим на этих линиях прямоугольники, используя линии
как стороны.Если при этом прямоугольник рисовать
вправо от пересечения линии и прямой - метод
левых прямоугольников(хотя слева не прямоуг.,а линия)
если влево - то правых прямоугольников (парадокс)}
Uses Crt;
Var
   Prav, Lev, X, s, R: real;
   n:INTEGER;

Begin
     ClrScr;
     Writeln('Вычисление интеграла методами левых и правых прямоугольников');
     Prav:= 0; {на всякий пожарный случай обнулим переменные}
     Lev:= 0;
     s:= 2 / 10; {шаг=пределы интегр-я / кол-во разбиений}
     for n:= 1 to 10 do
     {метод левых прямоуг.}
     begin
          x:= s * (n - 1);
          Lev:= Lev + s * x * EXP(-x);
          {Lev=тек.зн-е + x*e^(-x), х на данной итерациии
          х = а+s*n,где а-нижн.предел(в данн.случае 0),
          s-шаг, n - номер шага расчета
          метод правых прямоуг.}
          x:= s * n;
          Prav:= Prav + s * x * EXP(-x)
          {Prav=тек.зн-е + x*e^(-x), х на данной итерациии
          х = а+s*n,где а-нижн.предел(в данн.случае 0),
          s-шаг, n - номер шага расчета}
     end;
     Writeln;
     Writeln('При вычислении интеграла по методу правых прямоугольников I=', Prav:6:2);
     Writeln('по методу левых прямоугольников I=', Lev:6:2);
     R:= ABS(Prav - Lev);  {разность}
     Write('Результат, полученный методом ');
     {после  следующий PRINT выведется в ту же строку}
     if (Prav > Lev) then Write('правых')
     else Write('левых');
     Writeln(' прямоугольников больше на ', R:6:2);
End.
