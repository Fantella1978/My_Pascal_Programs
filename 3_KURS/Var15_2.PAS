{Сглаживание}
Uses crt;
Var
 x, y, ys : array [0..19] of real;
 i: integer;

Begin
for i:=0 to 19 do
  begin
       x[i]:= 0.1 + 0.05 * i; {набираем массив аргументов}
       ys[i]:= (EXP(x[i]) + EXP(-x[i])) / 2; {вычисляем
                                        значение ф-ции для аргумента}
       y[i]:= ys[i]                     {заполняем другой такой же массив,
                                        для сохранения начальных значений}
  end;

{дальше - просто формулы
объяснять их, наверное, не нужно
можно сказать что производим
усреднение ряда y(i) на основании
интерполяционных полиномов}
   ys[0]:= (3 * ys[0] + 2 * ys[1] + ys[2] - ys[4]) / 5;
   ys[1]:= (4 * ys[0] + 3 * ys[1] + 2 * ys[2] + ys[3]) / 10;

   for i:= 2 to 17 do
       ys[i]:= (ys[i-2] + ys[i-1] + ys[i] + ys[i+1] + ys[i+2]) / 5;

   ys[18]:= (ys[16] + 2 * ys[17] + 3 * ys[18] + 4 * ys[19]) / 10;
   ys[19]:= (3 * ys[19] + 2 * ys[18] + ys[17] - ys[16]) / 5;

   {Выводим результаты  и исходные данные}
   for i:= 0 to 19 do
       writeln( 'X(',i,')=', X[i]:6:2, ';   Y(',i,')=', Y[i]:6:2, ';     Ys(',
                i,')', Ys[i]:6:2 )
End.